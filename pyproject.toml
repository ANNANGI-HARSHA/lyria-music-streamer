[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lyria-mcp"
version = "0.1.0"
description = "Lyria MCP server with Gemini integration"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_paths = ["."]